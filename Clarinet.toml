[project]
name = "skill-nexus-registry"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/skill-nexus-registry/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.skill-nexus-registry]
path = "contracts/skill-nexus-registry.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
